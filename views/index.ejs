<%- include('partials/header'); -%>
    <h1>Home</h1>
    <p>This is home page.</p>
    <%posts.forEach(e=>{%>
        <h1><%= e.title %> </h1>
        <p><%=e.body.substring(0, 100)%> <a href="/post/<%= e._id %>">View Details</a> </p>
    <%})%>
<%- include('partials/footer'); -%>